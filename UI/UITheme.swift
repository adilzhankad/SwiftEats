import SwiftUI

enum UITheme {
    static let primary = Color(hex: "#FF8B42")
    static let primaryDarker = Color(hex: "#F06E1F")

    static let bg = Color(hex: "#F7F7F7")
    static let card = Color.white
    static let textMain = Color.primary
    static let shadow = Color.black.opacity(0.08)
}
